model Staff {
    id       String       @id @default(uuid())
    password String
    username String       @unique
    role     StaffRole    @default(STAFF)
    events   StaffEvent[]

    @@map("staffs")
}
